# 如何发布 Brain & Bytes 文章

> Brain & Bytes（知觉档案）站中站的发文指南。
> 线上地址：https://luoziyan100.github.io/brain-bytes/

---

## 🚀 发一篇新文章（3 步）

```bash
# 1. 把生成好的 HTML 放进去，目录名 = 文章英文 slug（小写、连字符）
#    例：public/brain-bytes/theta-rhythms-in-memory/index.html
cp 你的文章.html public/brain-bytes/<英文-slug>/index.html

# 2. 提交并推送
git add -A
git commit -m "post: 文章标题"
git push

# 3. 等 1~2 分钟，GitHub Actions 自动构建 + 部署
#    新文章自动出现在 https://luoziyan100.github.io/brain-bytes/
```

**就这么多。** 索引、返回栏、元数据抽取全部自动完成，无需手工改任何列表或配置。

---

## 📋 对 HTML 的唯一要求

文章是**自包含 HTML**（内联 CSS，排版随意），但必须保留两处结构，索引器靠它们抽取信息：

1. **标题** —— 一个 `<h1>...</h1>`
2. **元信息块** —— 一个 `class="meta"` 的区块，内含这几行（emoji 是识别标记）：

```html
<div class="meta">
  <span>📄 论文英文标题</span><br>
  <span>✍️ 作者列表</span><br>
  <span>🏛️ 机构</span><br>
  <span>📅 来源 · 年份</span>
</div>
```

> 现有模板（红色学术风）天然符合，直接用即可。

---

## ⚙️ 背后发生了什么

`npm run build` 会依次：

1. `posts:index` —— 生成技术博客索引
2. `bytes:index`（`scripts/generate-bytes-index.js`）—— 扫描 `public/brain-bytes/*/index.html`：
   - 抽取标题/论文/作者/机构/来源 → 写入 `public/brain-bytes/index.json`
   - 向每篇 HTML 顶部**幂等注入**「← Brain & Bytes · 知觉档案」返回栏
3. `vite build` —— 构建 SPA
4. 拷贝 `index.html` → `404.html`（SPA 深链兜底）
5. 拷贝 `index.html` → `brain-bytes/index.html`（让 `/brain-bytes/` 启动列表页，避免目录与路由相撞）

GitHub Actions（`.github/workflows/deploy.yml`）在每次 push 到 `main` 时自动跑这套并发布。

---

## 🔍 本地预览

- **准确预览**（和线上一致）：`npm run build && npm run preview` → http://localhost:4173/
- ⚠️ **不要用 `npm run dev` 预览文章**：dev 服务器不为 `public/` 子目录做目录索引，点文章会空白。这是 dev 专属局限，**不影响线上**。

---

## 🗂️ 相关文件

| 路径 | 作用 |
|---|---|
| `public/brain-bytes/<slug>/index.html` | 文章本体（自包含 HTML） |
| `public/brain-bytes/index.json` | 自动生成的文章索引（勿手改） |
| `scripts/generate-bytes-index.js` | 索引器 + 返回栏注入 |
| `src/pages/BrainBytesPage.jsx` | 列表首页（红色学术风） |
| `src/pages/brain-bytes.css` | 列表页专属样式 |
| `.github/workflows/deploy.yml` | 自动构建 + 部署 |
